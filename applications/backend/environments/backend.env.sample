# CORS configuration for frontend access.
# List of allowed origins (development frontends on localhost).
CORS_ALLOW_ORIGINS=["http://localhost:4173", "http://localhost:5173"]

# Local server runtime configuration.
# Port and timeout for Gunicorn, and log verbosity level.
APPLICATION_PORT=8000
GUNICORN_TIMEOUT=10
LOGLEVEL=info